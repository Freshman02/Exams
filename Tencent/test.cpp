#include <iostream>
using namespace std;
#include <vector>
#include <cmath>
#include <string>

int main()
{
    // vector<int> a;
    // a.push_back(2);
    // a.push_back(3);
    // a.push_back(4);
    // a.push_back(5);
    // a.push_back(6);
    // a.pop_back();

    // for (auto it = a.begin(); it != a.end(); ++it)
    // {
    //     cout << *it << " ";
    // }


    // cout << sqrt(4) << endl;
    
    
    cout << (323 & 1) << endl;
    string s = to_string(101);
    
    s += "hello";
    cout << s << endl;
    s.p
    
    
    return 0;
}